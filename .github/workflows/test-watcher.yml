# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: Test Watcher

on:
  push:
    branches:
      - "**"
    paths:
      - "watcher/**"
      - "shared/**"
  pull_request:
    branches:
      - "**"
    paths:
      - "watcher/**"
      - "shared/**"

jobs:
  test:
    name: watcher テスト
    runs-on: ubuntu-latest

    steps:
      - name: リポジトリをチェックアウト
        uses: actions/checkout@v4

      - name: Task をインストール
        uses: arduino/setup-task@v2
        with:
          version: 3.x

      - name: watcher サービスのテストを実行
        run: task test:watcher

      - name: テスト用コンテナを削除
        if: always()
        run: docker compose -f docker-compose-test.yaml down -v
